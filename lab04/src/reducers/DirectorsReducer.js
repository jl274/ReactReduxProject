const initState = {
    list: []
}

const directorsReducer = (state = initState, action) => {
    switch(action.type){

        default:
            return state;
    }
}

export default directorsReducer;